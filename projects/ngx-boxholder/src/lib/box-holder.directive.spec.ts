import { BoxHolderDirective } from './box-holder.directive';

describe('BoxHolderDirective', () => {
  it('should create an instance', () => {
    const directive = new BoxHolderDirective();
    expect(directive).toBeTruthy();
  });
});
